<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:name=".StationlyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Stationly"
        tools:targetApi="31">

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.Stationly.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="stationly" android:host="auth" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="stationly" android:host="reset" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="stationly" android:host="home" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="stationly" android:host="verified" />
            </intent-filter>
        </activity>

        <!-- FCM Messaging Service -->
        <service
            android:name=".service.FcmMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Daydream / Screensaver — appears in Settings → Display → Screensaver
             as "Stationly". exported=true + BIND_DREAM_SERVICE permission are
             required by the system so only the OS can bind us. -->
        <service
            android:name=".dream.StationlyDreamService"
            android:exported="true"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_DREAM_SERVICE">
            <intent-filter>
                <action android:name="android.service.dreams.DreamService" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <meta-data
                android:name="android.service.dream"
                android:resource="@xml/stationly_dream_info" />
        </service>

        <!-- Configuration screen for the Daydream. Reached via the gear icon
             next to "Stationly" in Settings → Display → Screensaver.

             - taskAffinity="" + launchMode="singleTask": isolate from the
               main app task. Without this, system Settings stacks this
               activity onto Stationly's existing task, and swiping back
               navigates through MainActivity and the rest of the app
               history instead of returning to Settings. Empty affinity
               keeps it in its own task; singleTask makes Android open it
               cleanly even if the app is already running in the background.
             - excludeFromRecents="true": this is a config screen invoked
               by system Settings, not user-facing app navigation. Shouldn't
               appear as its own recents entry. -->
        <activity
            android:name=".dream.DreamSettingsActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Stationly"
            android:taskAffinity=""
            android:launchMode="singleTask"
            android:excludeFromRecents="true" />

        <!-- Widget Provider -->
        <receiver
            android:name=".widget.DepartureWidgetProvider"
            android:exported="false"
            android:label="@string/widget_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.stationly.mobile.ACTION_UPDATE_WIDGET" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/departure_widget_info" />
        </receiver>

        <!-- WorkManager for background updates -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="com.stationly.mobile.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup" />
        </provider>

    </application>

</manifest>